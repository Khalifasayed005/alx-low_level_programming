#include <stdio.h>

/**
 * main - prints the argument
 * @argc: int
 * @argv: list
 * Return: 0-success, non-zero-fail
*/

int main(int argc, char const *argv[])
{
(void)argv;
printf("%d\n", argc - 1]);
return (0);
}
